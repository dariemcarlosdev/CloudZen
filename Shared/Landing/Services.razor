@using CloudZen.Models
@using CloudZen.Services
@inject PersonalService PersonalService

<section id="services" class="py-24 bg-gray-50 text-center">
    <h2 class="text-4xl font-bold mb-4 bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">How I Can Help</h2>
    <h3 class="text-xl font-semibold mb-4">Less Hassle. More Growth.</h3>
    <p class="text-gray-600 text-lg max-w-2xl mx-auto mb-10">
        Whether you're stuck with slow systems, drowning in manual tasks, or just need technology that works — I've got you covered. Here's what I bring to the table:
    </p>
    <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        @foreach (var service in _services)
        {
            <div class="p-6 bg-white rounded-xl shadow hover:shadow-lg transition-transform duration-300 hover:-translate-y-2">
                <div class="text-4xl mb-4">@service.Icon</div>
                <h3 class="text-xl font-semibold mb-2">@service.Title</h3>
                <p class="text-gray-600">@service.Description</p>
            </div>
        }
    </div>
</section>

@code {
    private List<ServiceInfo> _services = new();

    protected override void OnInitialized()
    {
        _services = PersonalService.GetAllServices();
    }
}
