<section id="hero" class="relative bg-white min-h-[60vh] flex items-center justify-center">
    <!-- Changed min-h-screen to min-h-[60vh] to reduce hero section height -->
    <div class="absolute inset-0 pointer-events-none select-none" aria-hidden="true">
        <div class="absolute -top-32 -right-32 w-96 h-96 rounded-full bg-blue-50 blur-3xl"></div>
        <div class="absolute -bottom-32 -left-32 w-96 h-96 rounded-full bg-indigo-50 blur-3xl"></div>
    </div>

    <div class="relative max-w-7xl w-full px-6 flex flex-col lg:flex-row items-center justify-center gap-16">
        <div class="flex-1 text-center lg:text-left flex flex-col items-center lg:items-start">
            <!-- Increased top margin for h1 on mobile: mt-24 (sm:mt-6 for larger screens) -->
            <h1 class="text-4xl sm:text-5xl font-bold tracking-tight text-gray-900 mb-6 mt-24 sm:mt-6 font-ibmplex">
                To Test Empowering Businesses with .NET, Cloud & AI
            </h1>
            <p class="text-lg sm:text-xl text-gray-600 leading-relaxed max-w-2xl mx-auto lg:mx-0 mb-8">
                I partner with organizations to <span class="font-bold text-indigo-700">modernize applications</span>, <span class="font-bold text-indigo-700">streamline delivery pipelines</span>, and integrate <span class="font-bold text-indigo-700">AI-driven automation</span> using <span class="font-bold text-indigo-700">Blazor</span>, <span class="font-bold text-indigo-700">ASP.NET Core</span>, and <span class="font-bold text-indigo-700">Azure</span>.
                From strategy to execution <span class="font-bold text-indigo-700">faster</span>, <span class="font-bold text-indigo-700">smarter</span>, and <span class="font-bold text-indigo-700">future-ready</span>.
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center items-center w-full">
                <a href="#contact" class="px-8 py-4 rounded-lg bg-indigo-600 text-white font-semibold shadow hover:bg-indigo-700 transition">
                    Book a Consultation
                </a>
                <a href="#services" class="px-8 py-4 rounded-lg bg-gray-100 text-gray-800 font-semibold hover:bg-gray-200 transition">
                    Explore Services
                </a>
            </div>
        </div>
        <div class="flex-1 grid grid-cols-2 gap-6 w-full max-w-md">
            <div class="p-6 rounded-xl bg-white shadow border border-gray-100 flex flex-col items-center text-center">
                <div class="text-3xl mb-3">⚡</div>
                <h3 class="font-semibold mb-1">Performance</h3>
                <p class="text-sm text-gray-600">Optimized, scalable architectures.</p>
            </div>
            <div class="p-6 rounded-xl bg-white shadow border border-gray-100 flex flex-col items-center text-center">
                <div class="text-3xl mb-3">🔐</div>
                <h3 class="font-semibold mb-1">Security</h3>
                <p class="text-sm text-gray-600">Best practices & defense in depth.</p>
            </div>
            <div class="p-6 rounded-xl bg-white shadow border border-gray-100 flex flex-col items-center text-center">
                <div class="text-3xl mb-3">🤖</div>
                <h3 class="font-semibold mb-1">Intelligence</h3>
                <p class="text-sm text-gray-600">AI & automation woven in.</p>
            </div>
            <div class="p-6 rounded-xl bg-white shadow border border-gray-100 flex flex-col items-center text-center">
                <div class="text-3xl mb-3">🚀</div>
                <h3 class="font-semibold mb-1">Velocity</h3>
                <p class="text-sm text-gray-600">CI/CD & DevOps acceleration.</p>
            </div>
        </div>
    </div>
</section>

@code {
    // Hero simplified to avoid duplicating Services component content.
}
